from django.contrib import admin
from .models import teacher_models,teacher_video_models

# Register your models here.
admin. site. register(teacher_models)
admin. site. register(teacher_video_models)


